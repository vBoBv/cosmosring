import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
	starParticles: {
		position: 'absolute',
		top: 0,
		left: 0,
		height: '100vh',
		width: '100%'
	}
}));
